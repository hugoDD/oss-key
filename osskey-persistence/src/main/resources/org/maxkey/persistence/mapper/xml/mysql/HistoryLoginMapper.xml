<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.maxkey.persistence.mapper.HistoryLoginMapper" >

	<sql id="dao_where_statement">
    	<if test="historyLogin.id != null and historyLogin.id != ''">
			AND	ID	=	#{id}
		</if>
		<if test="historyLogin.uid != null and historyLogin.uid != ''">
			AND	UID	=	#{historyLogin.uid}
		</if>
		<if test="historyLogin.username != null and historyLogin.username != '' ">
			AND LOWER(USERNAME) LIKE LOWER(CONCAT('%',#{historyLogin.username},'%'))
		</if>
		<if test="historyLogin.message != null and historyLogin.message != '' ">
			AND LOWER(MESSAGE) LIKE LOWER(CONCAT('%',#{historyLogin.message},'%'))
		</if>
		<if test="historyLogin.displayName != null and historyLogin.displayName != '' ">
			AND LOWER(DISPLAYNAME) LIKE LOWER(CONCAT('%',#{historyLogin.displayName},'%'))
		</if>

		<if test="historyLogin.startDate != null and historyLogin.startDate != ''">
			AND	LOGINTIME	>=	#{historyLogin.startDate}
		</if>
		<if test="historyLogin.endDate != null and historyLogin.endDate != ''">
			AND	 #{historyLogin.endDate}	>=	LOGINTIME
		</if>
		<if test="historyLogin.sessionId != null and historyLogin.sessionId != ''">
			AND	SESSIONID	=	#{historyLogin.sessionId}
		</if>
		<if test="historyLogin.loginType != null and historyLogin.loginType != ''">
			AND	LOGINTYPE	=	#{historyLogin.loginType}
		</if>
		<if test="historyLogin.sourceIp != null and historyLogin.sourceIp != ''">
			AND	SOURCEIP	=	#{historyLogin.sourceIp}
		</if>
    </sql>



     <select id="queryPageResults" parameterType="HistoryLogin" resultType="org.maxkey.domain.HistoryLogin">
    	SELECT
			ID,
	   		SESSIONID,
			UID,
			USERNAME,
			DISPLAYNAME,
			LOGINTYPE,
			MESSAGE,
			CODE,
			PROVIDER,
			SOURCEIP,
			BROWSER,
			PLATFORM,
			APPLICATION,
			LOGINURL,
			DATE_FORMAT(LOGINTIME, '%Y-%m-%d  %H:%i:%s') AS LOGINTIME,
			DATE_FORMAT(LOGOUTTIME, '%Y-%m-%d  %H:%i:%s') AS LOGOUTTIME
    	FROM MXK_HISTORY_LOGIN
    	WHERE	1	=	1
    	<include refid="dao_where_statement"/>

    	ORDER BY LOGINTIME DESC
    </select>

    <update id="logout" parameterType="java.lang.String" >
    	UPDATE MXK_HISTORY_LOGIN SET
			LOGOUTTIME			=	current_timestamp()
		WHERE
			SESSIONID	=	#{value}
    </update>
</mapper>
